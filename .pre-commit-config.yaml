# Pre-push: run server and webapp tests. Install with: pre-commit install --hook-type pre-push
repos:
  - repo: local
    hooks:
      - id: server-webapp-tests
        name: server and webapp tests
        entry: bash scripts/pre-push-tests.sh
        language: system
        pass_filenames: false
        stages: [pre-push]
