{
  "name": "@aiqa/mcp-server",
  "version": "0.9.2",
  "description": "MCP server for AIQA - provides tools for working with datasets, examples, experiments, and traces",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "start:local": "node --env-file=.env dist/index.js",
    "test": "pnpm run build && node --loader ts-node/esm test/unit/client.test.ts",
    "test:unit": "pnpm run build && node --loader ts-node/esm test/unit/client.test.ts",
    "test:integration": "pnpm run build && node --loader ts-node/esm test/integration/integration.test.ts",
    "test:protocol": "pnpm run build && node --loader ts-node/esm test/integration/mcp-protocol.test.ts"
  },
  "dependencies": {
    "@fastify/cors": "^11.3.0",
    "@modelcontextprotocol/sdk": "1.30.0",
    "fastify": "^5.12.3"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  }
}
